# Requirements for PlanTree application# Apache License 2.0# Core dependenciesPySide6>=6.5.0numpy>=1.24.0sounddevice>=0.4.6# PyAudio>=0.2.11# Optional dependencies for additional functionality# pyaudio backend for sounddevice (recommended for better compatibility)# No explicit dependency needed as sounddevice uses PortAudio# Development dependencies (optional)# pytest>=7.0.0# black>=22.0.0# flake8>=6.0.0# Platform-specific notes:# On Linux: may require additional system packages#   sudo apt-get install libportaudio2 portaudio19-dev python3-dev#   or equivalent for your distribution# On Windows: no additional requirements# On macOS: may require portaudio via Homebrew#   brew install portaudio